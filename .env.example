# Copy to .env and customize.
# Verify host UID/GID with: id -u; id -g

UID=1000
GID=1000

# Host path of 2nd-brain data root (WSL2 ext4 native).
# Container always mounts to /workspace/2nd-brain (do not change).
SB_DATA=${HOME}/projects/2nd-brain
